{
    "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "Container": { "type": "object" },
      "recoveryVault": { "type": "object" },
      "policyName": { "type": "string" },
      "count": { "type": "int" },
      "vm": { "type": "object" }
    },
    "variables": {},
    "resources": [
      {
        "name": "[concat(resourceId(subscription().subscriptionId,parameters('recoveryVault').groupName,'Microsoft.RecoveryServices/vaults',parameters('recoveryVault').name), '/backupFabrics/', parameters('Container').backupFabric, '/protectionContainers/', parameters('Container').v2VmContainer, concat(resourceGroup().name,';',parameters('vm').name,'-',copyIndex(1)), '/protectedItems/', parameters('Container').v2Vm, concat(resourceGroup().name,';',parameters('vm').name,'-',copyIndex(1)))]",
        "apiVersion": "2016-06-01",
        "location": "[resourceGroup().location]",
        "type": "Microsoft.RecoveryServices/vaults/backupFabrics/protectionContainers/protectedItems",
        "copy": {
          "name": "v2VmsCopy",
          "count": "[parameters('count')]"
        }
      }
    ],
    "outputs": { }
}